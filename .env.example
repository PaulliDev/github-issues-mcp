# GitHub token with Issues read/write on the target repo (fine-grained PAT recommended)
GITHUB_TOKEN=github_pat_your_token_here
GITHUB_OWNER=your-org
GITHUB_REPO=your-repo

# Only needed for the HTTP server (npm run start:http)
MCP_AUTH_TOKEN=generate-a-long-random-string
PORT=3001
